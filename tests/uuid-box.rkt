#lang racket

(require libuuid
         rackunit
         "../uuid-box.rkt")

(module+ test
  (test-case "fetch-uuid-box"
    (fail "write test"))
  
  (test-case "uuid-unbox"
    (fail "write test"))
  
  (test-case "uuid-unbox/refresh"
    (fail "write test"))
  
  (test-case "set-uuid-box!"
    (fail "write test")))